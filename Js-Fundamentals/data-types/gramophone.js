function solve(band, album, song) {
    let rotadeCount = 0;

    let time = ((Number(band.length) * Number(album.length)) * Number(song.length)) / 2;

    console.log(`The plate was rotated ${rotadeCount = Math.ceil(time / 2.5)} times.`);
}
solve(`Black Sabbath`, 'Paranoid', 'War Pigs')