function projectCreation(Name, projectNum) {
    for (let projectNum = 0; projectNum <= 100; projectNum++);
    let b = Number(3);
    let hours = projectNum * b;
        console.log(`The architect ${Name} will need ${hours} hours to complete ${projectNum} project/s.`);

}

projectCreation("niko", 99);

//