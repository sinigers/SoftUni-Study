function srtingToRow(strings) {

    let string = strings;
    // let letter = text.charAt(4);

    for (let i = 0; i < string.length; i++) {
        console.log(string[i])
    }

    // console.log(string);
}

srtingToRow("softuni");