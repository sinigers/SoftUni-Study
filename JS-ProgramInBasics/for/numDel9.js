function deltoToNine(numFrom, numTo) {
    let numFr=Number(numFrom);
    let numT=Number(numTo);

let sum=0;
let numOutput="";


for (let index = numFr; index < numT; index++) {
    
    if (index % 9 === 0){
sum += numFr(index);
numOutput = 
    
    console.log(sum);
    // console.log(numOutput);
}
}


deltoToNine("100", "200")